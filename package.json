{
  "name": "swing360-root",
  "version": "1.0.0",
  "description": "Root delegation for Swing 360 project",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run dev --prefix frontend",
    "backend": "cd backend && python app.py"
  },
  "engines": {
    "node": ">=20"
  }
}
